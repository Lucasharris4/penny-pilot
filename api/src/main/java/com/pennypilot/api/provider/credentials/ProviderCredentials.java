package com.pennypilot.api.provider.credentials;

/**
 * Marker interface for provider-specific credentials.
 * Each provider defines its own implementation with the fields it needs.
 * MockProvider receives null (no credentials required).
 */
public interface ProviderCredentials {
}
